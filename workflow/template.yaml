project:
  name: placeholder
input:
  h5ad: /placeholder.h5ad
  counts_layer: counts
compute:
  backend: auto
  fallback_to_cpu: true
preprocessing:
  normalization:
    # The pipeline owns its namespaced output layer; re-deriving it must not
    # error when an input already carries a prior cellquorum_normalized layer.
    overwrite: true
run:
  random_seed: 1337
  resume: true
report:
  enabled: true
  html: true
  markdown: true
